<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Traces: BASED-INSTALLER-MAC — merged into the generated Info.plist by the Tauri bundler.
     Registers based as an *available* handler for .sql (LSHandlerRank Alternate) without stealing
     the user's existing default — the same non-destructive stance BASED-SQL-ASSOC-WIN takes with
     HKCU OpenWithProgids on Windows. Double-clicks/"Open With" arrive as Apple Events
     (RunEvent::Opened in main.rs), never argv. -->
<plist version="1.0">
<dict>
  <key>CFBundleDocumentTypes</key>
  <array>
    <dict>
      <key>CFBundleTypeExtensions</key>
      <array>
        <string>sql</string>
      </array>
      <key>CFBundleTypeName</key>
      <string>SQL file</string>
      <key>CFBundleTypeRole</key>
      <string>Editor</string>
      <key>LSHandlerRank</key>
      <string>Alternate</string>
    </dict>
  </array>
</dict>
</plist>
