{
  "name": "@based/shell-tauri",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "bundle": "bun bundle-core.ts",
    "dev": "bun ensure-dist-core.ts && cargo run",
    "build": "bun bundle-core.ts && bun x @tauri-apps/cli build",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@based/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
