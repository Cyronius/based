// Unused stub — the real UI is served from the core server over localhost HTTP
// (see src/bun/index.ts). Electrobun's build config wants a views entry; this satisfies it.
console.log("based shell stub view");
