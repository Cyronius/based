// Unused placeholder view entrypoint — the spike window loads http://127.0.0.1:<port>
// from Bun.serve instead (the real app's architecture). Electrobun's build wants a
// views entry, so this file exists.
console.log("mainview placeholder loaded");
