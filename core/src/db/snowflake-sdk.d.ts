// Intentionally empty: snowflake-sdk ships its own typings (dist/index.d.ts), so redeclaring the
// module here would shadow them with a worse copy. Kept as a marker so nobody re-adds one — if the
// SDK's own types are ever missing a member the adapter needs, widen at the call site instead.
export {};
