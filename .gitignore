.claude/plans/
node_modules/
dist/
build/
*.log
.env
.env.*

# Vendored prebuilt dependency (lm-ag-ui) — the dist IS the package, keep it tracked despite dist/ above.
!ui/vendor/lm-ag-ui/dist/
!ui/vendor/lm-ag-ui/dist/**

.codegraph
*.tsbuildinfo
shell/artifacts/
shell-tauri/target/
shell-tauri/dist-core/
shell-tauri/gen/
shell-tauri/icons/android/
shell-tauri/icons/ios/
.claude/plans/

# Private keys must never be committed. Generate them outside the repo (~/.snowflake/); this is a
# backstop for when they land here anyway.
*.p8
*.pem
*.key
*_rsa_key*

# The paid product (based.ai) lives in a sibling repo, never here. Backstop for scripts/check-private.ts.
based-ai/
paid/
private/
*.private/
